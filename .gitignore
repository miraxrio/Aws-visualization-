# OSIRIS (vendored Next.js app) build & dependency artifacts
osiris/node_modules/
osiris/.next/
osiris/out/
osiris/build/
osiris/.vercel/

# Environment files (never commit secrets / API keys)
.env
.env.local
osiris/.env
osiris/.env*.local

# ZeroBias local credentials (API keys) — never commit; loaded at runtime only
data/zerobias/credentials.local.json
**/*.local.json

# OS / editor noise
.DS_Store
*.log
