## Git Workflow
- Do NOT create new branches. Always work directly on the branch that was checked out at the start of the session.
- Do NOT switch branches or run `git checkout -b`.
- Commit and push directly to the current branch only.
